#include "fonction_al.h"


/*
R = permet de cree une zone de taille dependante d unombre en entrer
E = la taille de la zone a crée
S = un pointeur
*/
float* Zone(unsigned int t)
{
	float* n_zone = new float[t];
	return n_zone;
}